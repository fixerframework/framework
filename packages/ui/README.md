# `@fixerframework/ui`

Preact UI primitives and state bridges (`Show`, `Await`, `Match`, `bind`).

```bash
bun add @fixerframework/ui
```

```ts
import { Button } from "@fixerframework/ui";
import "@fixerframework/ui/theme.css";
```
