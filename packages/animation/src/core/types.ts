/**
 * Internal re-export of animation types from `@fixerframework/types`.
 * Public consumers should import types from `@fixerframework/types` / `@fixerframework/types/animation`.
 */
export type {
  TransformKey,
  StyleKey,
  Target,
  Variant,
  Variants,
  TargetAndTransition,
  EasingName,
  Easing,
  SpringTransition,
  TweenTransition,
  Transition,
  AnimationPlaybackControls,
  ReducedMotionSetting,
  DragConstraints,
  DragAxis,
  MotionProps,
} from "@fixerframework/types/animation";
