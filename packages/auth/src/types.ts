/**
 * Internal re-export of auth client types from `@fixerframework/types`.
 * Public consumers should import from `@fixerframework/types` / `@fixerframework/types/auth`.
 */
export type { AuthRuntime, ClerkAuthRuntime, CreateClerkAuthConfig } from "@fixerframework/types/auth";
