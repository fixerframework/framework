/**
 * Internal re-export of state types from `@fixerframework/types`.
 * Public consumers should import types from `@fixerframework/types` / `@fixerframework/types/state`.
 */
export type {
  AuthRuntime,
  QueryKey,
  QueryStatus,
  QueryScope,
  FetchContext,
  QueryDef,
  MutateDef,
  Query,
  Atom,
  CreateStateOptions,
  StateRuntime,
} from "@fixerframework/types/state";
